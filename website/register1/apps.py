from django.apps import AppConfig




class Register1Config(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'register1'

   #this block of code is to allow configuration of the settings for the application "register1"